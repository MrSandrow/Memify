import styled from 'styled-components';

const ScrollWrapper = styled.div`
  height: 100%;
  left: 0;
  overflow-y: auto;
  position: absolute;
  top: 0;
  width: 100%;
`;

export default ScrollWrapper;
