import {
  Download,
  LogIn,
  LogOut,
  Menu,
  Search,
  Trash,
  User,
} from '@styled-icons/feather';

const icons = {
  download: Download,
  login: LogIn,
  logout: LogOut,
  menu: Menu,
  search: Search,
  trash: Trash,
  user: User,
};

export default icons;
