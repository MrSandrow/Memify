import {
  ArrowLeftCircle,
  LogOut,
  Menu,
  Save,
} from '@styled-icons/feather';

const icons = {
  arrowLeftCircle: ArrowLeftCircle,
  logout: LogOut,
  menu: Menu,
  save: Save,
};

export default icons;
