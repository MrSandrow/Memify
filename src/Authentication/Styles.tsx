import styled from 'styled-components';

const Wrapper = styled.div`
  display: grid;
  gap: 1.5em;
  grid-template-columns: min(25em, 85%);
  height: 100%;
  min-height: 35em;
  place-content: center;
`;

export default Wrapper;
